#!/usr/bin/env bash

if [ $# -gt 0 ]
then
    TITLE=$@
    SLUG=`tr '[:upper:] ' '[:lower:]-' <<< ${TITLE}`
    FILENAME=drafts/$SLUG.markdown
    if [ ! -e $FILENAME ]
    then
        echo -e "---\ntitle: ${TITLE}\ntags:\n---\n" > $FILENAME
    fi
fi
vim + $FILENAME
